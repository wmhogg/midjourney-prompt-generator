name: Deploy React Application

on:
  push:
    branches: [ main ]
  workflow_dispatch:

permissions:
  contents: read
  pages: write
  id-token: write

concurrency:
  group: "pages"
  cancel-in-progress: true

jobs:
  build:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout
        uses: actions/checkout@v4
      
      - name: Setup Node.js
        uses: actions/setup-node@v4
        with:
          node-version: '18'
          # Remove caching entirely to eliminate the dependency on lock files
          
      - name: Install dependencies
        # Simple, direct installation without lock file requirements
        run: npm install
        
      - name: Debug environment
        # Add diagnostic information to identify potential issues
        run: |
          echo "Repository structure:"
          ls -la
          echo "Node.js version:"
          node -v
          echo "NPM version:"
          npm -v
          echo "Current directory:"
          pwd
          
      - name: Build
        # Simplified build command with error output capture
        run: npm run build 2>&1 | tee build_output.log
        
      - name: Upload build logs if failure
        # Preserve build logs for diagnosis even if the build fails
        if: failure()
        uses: actions/upload-artifact@v4
        with:
          name: build-logs
          path: build_output.log
          
      - name: Setup Pages
        uses: actions/configure-pages@v4
        
      - name: Upload pages artifact
        uses: actions/upload-pages-artifact@v2
        with:
          path: ./build

  deploy:
    environment:
      name: github-pages
      url: ${{ steps.deployment.outputs.page_url }}
    runs-on: ubuntu-latest
    needs: build
    steps:
      - name: Deploy to GitHub Pages
        id: deployment
        uses: actions/deploy-pages@v2
